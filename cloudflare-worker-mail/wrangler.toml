name = "vlaggenweer-mail"
main = "mail.js"
compatibility_date = "2026-04-10"
account_id = "05da71c7c88b8ce49fbb2c2d0a570416"

# Custom subdomein — DNS-record + SSL-cert worden automatisch aangemaakt.
routes = [
  { pattern = "mail.weerlab.nl", custom_domain = true },
]

# RESEND_API_KEY wordt apart toegevoegd als secret via:
#   wrangler secret put RESEND_API_KEY
# (NIET hier in toml zetten — zou gecommit worden!)
